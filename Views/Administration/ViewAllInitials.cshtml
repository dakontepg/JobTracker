@model IEnumerable<JobTracker.Models.InitialsAssigned>

@{
    ViewData["Title"] = "View Initials";
}

<div class="container col-lg-8">

    <h2>Initials List</h2>

    <div>
        <a class="btn btn-primary" asp-controller="Administration" asp-action="AddInitials">Create New</a>
    </div>

    <table class="col-sm-12 stripe nowrap" id="viewInitialsTable">
        <thead>
            <tr>
                <th>Initials Assigned</th>
                <th>Status</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model){
                <tr>
                    <td>@item.InitialsAssignedName</td>
                    <td>@(item.Active ? "Active" : "Inactive")</td>
                    <td>
                        <a class="btn btn-primary" asp-controller="Administration" asp-action="EditInitials" asp-route-id="@item.InitialsAssignedId">Edit</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <div>
        <a asp-controller="Administration" asp-action="Index">Back to Admin Home Page</a>
    </div>

</div>

<link href="https://cdn.datatables.net/2.0.3/css/dataTables.dataTables.css" rel="stylesheet" />

@section scripts{
    <script src="https://cdn.datatables.net/2.0.3/js/dataTables.js"></script>
    <script>
        // Creates DataTable
        // DataTable Documentation found here: https://datatables.net/
        $(function () {
            $('#viewInitialsTable').DataTable({
                columns: [null,null, { orderable: false }],
                order: [[0, 'asc']],
                @* pageLength: 10,
                searching: true,
                lengthChange: false,
                language: {
                    emptyTable: 'No data found'
                } *@
            });
        });
    </script>
}